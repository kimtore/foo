# vi: se sts=2 ts=2 sw=2 et:

image: ambientsound/foo
team: aura
healthcheck:
  liveness:
    path: /health
  readiness:
    path: /health
port: 8080
prometheus:
  enabled: true
alerts:
  - alert: tooHighGauge
    expr: foo_main_gauge > 0.0
    for: 0m
    annotations:
      sms: true
      action: Stop hammering me!
    labels:
      severity: nonsense

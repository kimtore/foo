# vi: se sts=2 ts=2 sw=2 et:

image: ambientsound/foo
team: aura
healthcheck:
  liveness:
    path: /health
  readiness:
    path: /health
port: 8080
prometheus:
  enabled: true
alerts:
  - alert: tooManyHealthChecks
    expr: sum(rate(foo_main_health_requests[5m])) > 1.0
    for: 1m
    annotations:
      alertGroup: fo@a01mmfw030.adeo.no
      action: Stop hammering me!
    labels:
      severity: nonsense
